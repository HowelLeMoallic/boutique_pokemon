controllers:
    resource:
        path: ../src/Controller/
        namespace: App\Controller
    type: attribute

app_home:
    path: /
    controller: App\Controller\ArticleController::index
    methods: GET
    # Ajoutez la ligne suivante pour rediriger vers la page "Article"
    defaults:
        _redirect: article